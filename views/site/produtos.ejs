<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nossos Produtos - Café Belle Vie</title>
    <link rel="stylesheet" href="/css/site.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet">
    <style>
        .produtos-container {
            max-width: 1200px;
            margin: 40px auto;
            padding: 0 20px;
        }

        .produtos-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .produtos-header h1 {
            font-size: 2.5rem;
            color: #2c1810;
            margin-bottom: 15px;
        }

        .filtros {
            display: flex;
            gap: 20px;
            margin-bottom: 30px;
            flex-wrap: wrap;
            justify-content: center;
        }

        .filtros form {
            display: flex;
            gap: 10px;
        }

        .filtros input,
        .filtros select {
            padding: 10px 15px;
            border: 2px solid #c8a882;
            border-radius: 5px;
            font-size: 1rem;
        }

        .filtros button {
            background-color: #8b6f47;
            color: white;
            padding: 10px 25px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .filtros button:hover {
            background-color: #6d5636;
        }

        .grid-produtos {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 30px;
            margin-bottom: 50px;
        }

        .card-produto {
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s, box-shadow 0.3s;
            display: flex;
            flex-direction: column;
            cursor: pointer;
        }

        .card-produto:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }

        .produto-imagem {
            width: 100%;
            height: 200px;
            background: linear-gradient(135deg, #c8a882 0%, #8b6f47 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .produto-imagem img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .no-image {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            opacity: 0.3;
        }

        .produto-info {
            padding: 20px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        .produto-categoria {
            font-size: 0.8rem;
            color: #8b6f47;
            text-transform: uppercase;
            font-weight: bold;
            margin-bottom: 8px;
        }

        .produto-nome {
            font-size: 1.3rem;
            color: #2c1810;
            margin-bottom: 10px;
            font-weight: 600;
        }

        .produto-descricao {
            font-size: 0.9rem;
            color: #666;
            margin-bottom: 15px;
            flex-grow: 1;
        }

        .produto-rodape {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-top: 1px solid #eee;
            padding-top: 15px;
        }

        .produto-preco {
            font-size: 1.5rem;
            color: #8b6f47;
            font-weight: bold;
        }

        .btn-ver-mais {
            background-color: #8b6f47;
            color: white;
            padding: 8px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        .btn-ver-mais:hover {
            background-color: #6d5636;
        }

        .vazio {
            text-align: center;
            padding: 60px 20px;
            color: #666;
        }

        .vazio h2 {
            color: #2c1810;
            margin-bottom: 15px;
        }

        .cafeteria-badge {
            font-size: 0.75rem;
            background-color: #f5f5f5;
            color: #666;
            padding: 4px 8px;
            border-radius: 3px;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <div class="header-container">
            <div class="logo">
                <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M17 8h1a4 4 0 1 1 0 8h-1"/>
                    <path d="M3 8h14v9a4 4 0 0 1-4 4H7a4 4 0 0 1-4-4Z"/>
                    <line x1="6" x2="6" y1="2" y2="4"/>
                    <line x1="10" x2="10" y1="2" y2="4"/>
                    <line x1="14" x2="14" y1="2" y2="4"/>
                </svg>
                <h1><a href="/" style="color: white; text-decoration: none;">Café Belle Vie</a></h1>
            </div>

            <nav id="desktop-nav">
                <a href="/">Início</a>
                <a href="/produtos">Produtos</a>
                <a href="/cafeterias">Cafeterias</a>
                <a href="/contato">Contato</a>
                <a href="/admin" style="margin-left:12px;border-left:1px solid rgba(255,255,255,0.12);padding-left:12px;color:white;font-weight:600;">Admin</a>
            </nav>

            <button class="mobile-menu-btn" onclick="toggleMobileMenu()">
                <svg id="menu-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <line x1="3" x2="21" y1="6" y2="6"/>
                    <line x1="3" x2="21" y1="12" y2="12"/>
                    <line x1="3" x2="21" y1="18" y2="18"/>
                </svg>
            </button>
        </div>

        <nav id="mobile-nav" class="mobile-nav">
            <a href="/" onclick="toggleMobileMenu()">Início</a>
            <a href="/produtos" onclick="toggleMobileMenu()">Produtos</a>
            <a href="/cafeterias" onclick="toggleMobileMenu()">Cafeterias</a>
            <a href="/contato" onclick="toggleMobileMenu()">Contato</a>
            <a href="/admin" onclick="toggleMobileMenu()">Admin</a>
        </nav>
    </header>

    <!-- Produtos Section -->
    <div class="produtos-container">
        <div class="produtos-header">
            <h1>Nossos Produtos</h1>
            <p>Conheça nossa seleção especial de cafés e bebidas</p>
        </div>

        <!-- Filtros -->
        <div class="filtros">
            <form method="GET" action="/buscar" style="display: flex; gap: 10px;">
                <input type="text" name="nome" placeholder="Buscar por nome..." value="">
                <button type="submit">Buscar</button>
            </form>
        </div>

        <!-- Grid de Produtos -->
        <% if (produtos && produtos.length > 0) { %>
            <div class="grid-produtos">
                <% produtos.forEach(produto => { %>
                    <div class="card-produto" onclick="window.location.href='/produto/<%= produto._id %>'">
                        <div class="produto-imagem">
                            <% if (produto.imagem) { %>
                                <img src="/img/<%= produto.imagem %>" alt="<%= produto.nome %>">
                            <% } else { %>
                                <div class="no-image">☕</div>
                            <% } %>
                        </div>
                        <div class="produto-info">
                            <div class="produto-categoria"><%= produto.categoria %></div>
                            <div class="produto-nome"><%= produto.nome %></div>
                            <div class="produto-descricao"><%= produto.descricao.substring(0, 80) %>...</div>
                            <div class="cafeteria-badge">
                                📍 <%= produto.cafeteria ? produto.cafeteria.nome : 'Cafeteria' %>
                            </div>
                            <div class="produto-rodape">
                                <div class="produto-preco">R$ <%= produto.preco.toFixed(2) %></div>
                                <a href="/produto/<%= produto._id %>" class="btn-ver-mais">Ver Mais</a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="vazio">
                <h2>Nenhum produto encontrado</h2>
                <p>Tente refinar sua busca ou volte mais tarde.</p>
                <a href="/produtos" style="color: #8b6f47; text-decoration: none; margin-top: 20px; display: inline-block;">← Voltar</a>
            </div>
        <% } %>
    </div>

    <!-- Footer -->
    <footer style="background-color: #2c1810; color: white; text-align: center; padding: 30px; margin-top: 50px;">
        <p>&copy; 2024 Café Belle Vie - Todos os direitos reservados</p>
    </footer>

    <script>
        function toggleMobileMenu() {
            const mobileNav = document.getElementById('mobile-nav');
            const menuIcon = document.getElementById('menu-icon');
            mobileNav.classList.toggle('active');
            menuIcon.classList.toggle('active');
        }
    </script>
</body>
</html>
