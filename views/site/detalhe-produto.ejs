<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= produto.nome %> - Café Belle Vie</title>
    <link rel="stylesheet" href="/css/site.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet">
    <style>
        .detalhe-container {
            max-width: 1200px;
            margin: 40px auto;
            padding: 0 20px;
        }

        .breadcrumb {
            margin-bottom: 30px;
            color: #666;
        }

        .breadcrumb a {
            color: #8b6f47;
            text-decoration: none;
        }

        .breadcrumb a:hover {
            text-decoration: underline;
        }

        .detalhe-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            margin-bottom: 60px;
        }

        .imagem-grande {
            width: 100%;
            height: 400px;
            background: linear-gradient(135deg, #c8a882 0%, #8b6f47 100%);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .imagem-grande img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .no-image-grande {
            font-size: 6rem;
            opacity: 0.3;
        }

        .info-produto {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .categoria-badge {
            display: inline-block;
            background-color: #f5f5f5;
            color: #8b6f47;
            padding: 8px 15px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 15px;
            width: fit-content;
        }

        .nome-grande {
            font-size: 2.5rem;
            color: #2c1810;
            margin-bottom: 15px;
            font-family: 'Playfair Display', serif;
        }

        .cafeteria-info {
            background-color: #f9f9f9;
            padding: 15px;
            border-left: 4px solid #8b6f47;
            margin-bottom: 20px;
            border-radius: 5px;
        }

        .cafeteria-info h4 {
            margin: 0 0 8px 0;
            color: #2c1810;
        }

        .cafeteria-info p {
            margin: 0;
            color: #666;
            font-size: 0.9rem;
        }

        .preco-grande {
            font-size: 2rem;
            color: #8b6f47;
            font-weight: bold;
            margin: 20px 0;
        }

        .descricao-longa {
            font-size: 1.05rem;
            color: #555;
            line-height: 1.6;
            margin-bottom: 30px;
        }

        .acoes {
            display: flex;
            gap: 15px;
            margin-top: 30px;
            flex-wrap: wrap;
        }

        .btn-primario {
            background-color: #8b6f47;
            color: white;
            padding: 15px 30px;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s;
            flex: 1;
            min-width: 150px;
        }

        .btn-primario:hover {
            background-color: #6d5636;
        }

        .btn-secundario {
            background-color: white;
            color: #8b6f47;
            padding: 15px 30px;
            border: 2px solid #8b6f47;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s;
            flex: 1;
            min-width: 150px;
        }

        .btn-secundario:hover {
            background-color: #8b6f47;
            color: white;
        }

        .relacionados {
            margin-top: 60px;
            padding-top: 40px;
            border-top: 1px solid #eee;
        }

        .relacionados h2 {
            font-size: 1.8rem;
            color: #2c1810;
            margin-bottom: 30px;
            text-align: center;
        }

        .grid-relacionados {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
        }

        .card-pequeno {
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s;
            cursor: pointer;
        }

        .card-pequeno:hover {
            transform: translateY(-5px);
        }

        .card-pequeno-img {
            width: 100%;
            height: 150px;
            background: linear-gradient(135deg, #c8a882 0%, #8b6f47 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .card-pequeno-img img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .card-pequeno-info {
            padding: 15px;
        }

        .card-pequeno-nome {
            font-weight: bold;
            color: #2c1810;
            margin-bottom: 8px;
        }

        .card-pequeno-preco {
            color: #8b6f47;
            font-weight: bold;
        }

        @media (max-width: 768px) {
            .detalhe-grid {
                grid-template-columns: 1fr;
                gap: 30px;
            }

            .nome-grande {
                font-size: 1.8rem;
            }

            .preco-grande {
                font-size: 1.5rem;
            }

            .acoes {
                flex-direction: column;
            }

            .btn-primario,
            .btn-secundario {
                flex: none;
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <div class="header-container">
            <div class="logo">
                <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M17 8h1a4 4 0 1 1 0 8h-1"/>
                    <path d="M3 8h14v9a4 4 0 0 1-4 4H7a4 4 0 0 1-4-4Z"/>
                    <line x1="6" x2="6" y1="2" y2="4"/>
                    <line x1="10" x2="10" y1="2" y2="4"/>
                    <line x1="14" x2="14" y1="2" y2="4"/>
                </svg>
                <h1><a href="/" style="color: white; text-decoration: none;">Café Belle Vie</a></h1>
            </div>

            <nav id="desktop-nav">
                <a href="/">Início</a>
                <a href="/produtos">Produtos</a>
                <a href="/cafeterias">Cafeterias</a>
                <a href="/contato">Contato</a>
                <a href="/admin" style="margin-left:12px;border-left:1px solid rgba(255,255,255,0.12);padding-left:12px;color:white;font-weight:600;">Admin</a>
            </nav>

            <button class="mobile-menu-btn" onclick="toggleMobileMenu()">
                <svg id="menu-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <line x1="3" x2="21" y1="6" y2="6"/>
                    <line x1="3" x2="21" y1="12" y2="12"/>
                    <line x1="3" x2="21" y1="18" y2="18"/>
                </svg>
            </button>
        </div>

        <nav id="mobile-nav" class="mobile-nav">
            <a href="/" onclick="toggleMobileMenu()">Início</a>
            <a href="/produtos" onclick="toggleMobileMenu()">Produtos</a>
            <a href="/cafeterias" onclick="toggleMobileMenu()">Cafeterias</a>
              <a href="/contato" onclick="toggleMobileMenu()">Contato</a>
              <a href="/admin" onclick="toggleMobileMenu()">Admin</a>
        </nav>
    </header>

    <!-- Conteúdo Principal -->
    <div class="detalhe-container">
        <!-- Breadcrumb -->
        <div class="breadcrumb">
            <a href="/">Início</a> / <a href="/produtos">Produtos</a> / <span><%= produto.nome %></span>
        </div>

        <!-- Detalhe do Produto -->
        <div class="detalhe-grid">
            <!-- Imagem -->
            <div class="imagem-grande">
                <% if (produto.imagem) { %>
                    <img src="/img/<%= produto.imagem %>" alt="<%= produto.nome %>">
                <% } else { %>
                    <div class="no-image-grande">☕</div>
                <% } %>
            </div>

            <!-- Informações -->
            <div class="info-produto">
                <div>
                    <div class="categoria-badge"><%= produto.categoria %></div>
                    <h1 class="nome-grande"><%= produto.nome %></h1>
                    
                    <% if (produto.cafeteria) { %>
                        <div class="cafeteria-info">
                            <h4>📍 Disponível em:</h4>
                            <p><strong><%= produto.cafeteria.nome %></strong></p>
                            <% if (produto.cafeteria.endereco) { %>
                                <p><%= produto.cafeteria.endereco %></p>
                            <% } %>
                            <% if (produto.cafeteria.telefone) { %>
                                <p>Telefone: <%= produto.cafeteria.telefone %></p>
                            <% } %>
                        </div>
                    <% } %>

                    <div class="preco-grande">R$ <%= produto.preco.toFixed(2) %></div>

                    <div class="descricao-longa">
                        <%= produto.descricao %>
                    </div>
                </div>

                <div class="acoes">
                    <a class="btn-primario" href="/pedido?product=<%= produto._id %>">🛒 Fazer Pedido</a>
                    <button class="btn-secundario" onclick="window.location.href='/produtos'">← Voltar aos Produtos</button>
                </div>
            </div>
        </div>

        <!-- Produtos Relacionados -->
        <% if (relacionados && relacionados.length > 0) { %>
            <div class="relacionados">
                <h2>Produtos Relacionados</h2>
                <div class="grid-relacionados">
                    <% relacionados.forEach(rel => { %>
                        <div class="card-pequeno" onclick="window.location.href='/produto/<%= rel._id %>'">
                            <div class="card-pequeno-img">
                                <% if (rel.imagem) { %>
                                    <img src="/img/<%= rel.imagem %>" alt="<%= rel.nome %>">
                                <% } else { %>
                                    <div style="font-size: 2rem; opacity: 0.3;">☕</div>
                                <% } %>
                            </div>
                            <div class="card-pequeno-info">
                                <div class="card-pequeno-nome"><%= rel.nome %></div>
                                <div class="card-pequeno-preco">R$ <%= rel.preco.toFixed(2) %></div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        <% } %>
    </div>

    <!-- Footer -->
    <footer style="background-color: #2c1810; color: white; text-align: center; padding: 30px; margin-top: 50px;">
        <p>&copy; 2024 Café Belle Vie - Todos os direitos reservados</p>
    </footer>

    <script>
        function toggleMobileMenu() {
            const mobileNav = document.getElementById('mobile-nav');
            const menuIcon = document.getElementById('menu-icon');
            mobileNav.classList.toggle('active');
            menuIcon.classList.toggle('active');
        }

        function adicionarAoCarrinho() {
            alert('Funcionalidade de carrinho em breve! 🛒');
        }
    </script>
</body>
</html>
